/*
#include <stdio.h>
int main() {
	int h, m, nh, nm;
	scanf_s("%d %d", &h, &m);
	if (m >= 45) {
		nh = h;
		nm = m - 45;
	}
	else {
		if (h == 0) 
			nh = 23;

		else
			nh = h - 1;
		nm = m + 15;
	}
	printf("%d %d\n", nh, nm);
	return 0;
}
*/