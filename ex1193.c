/*
#include <stdio.h>
int main() {
	int x=1,n=1;
	scanf_s("%d", &x);
	if (x == 1) {
		printf("1/1\n");
		return 0;
	}
	while (x-n>0) {
		x -= n;
		n++;
	}
	n++;
	if ((n - 1) % 2)
		printf("%d/%d", n - x, x);
	else
		printf("%d/%d", x, n - x);
}
*/